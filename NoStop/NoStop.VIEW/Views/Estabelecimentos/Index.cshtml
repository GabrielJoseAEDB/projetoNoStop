@model IEnumerable<NoStop.MODEL.Estabelecimento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<p>
    @if (Session["usuarioLogadoID"] != null && Session["Role"].ToString() == "admin")
    {
        @Html.ActionLink("Create New", "Create")
    }
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Endereco)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Endereco)
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new { id = item.ID }) |
            @Html.ActionLink("Setores", "Index", "Setores", new { idEstab = item.ID }, null) |
            @if (Session["usuarioLogadoID"] != null && Session["Role"].ToString() == "admin")
            {
                @Html.ActionLink("Editar o estabelecimento", "Edit", new { id = item.ID }) <span>|</span>
                @Html.ActionLink("Editar acessos", "ExibirClientes", new { idEstab = item.ID }) <span>|</span>
                @Html.ActionLink("Deletar o estabelecimento", "Delete", new { id = item.ID }) <span>|</span>
            }
        </td>
    </tr>
}

</table>
